# {{name}} — implementation plan
*Constraint-satisfaction form: the code must result in the constraints below being true.*
*Companion (Tier-1 narrative): <one paragraph — what must the END USER feel / be able to do?
Every constraint below traces up to a beat of this narrative.>*

---

## 1. Guiding role
<one sentence: the single felt outcome every derivation is judged against.>

## 2. Constraint set (the code has to result in this)

<!-- gantry grammar (SPEC §6): numbered items `N. **Bold Name.** body`, optionally grouped
     under `### C-GROUP` headings. The bold lead name is the constraint's stable slug — name
     the MEANING, never the ordinal. Cite constraints as "in vN"; the set grows by amendment
     (SPEC §2). Delete this comment when the first constraint lands. -->

<!-- example of the exact shape (uncomment and replace):
     ### C-CORE
     1. **Short Name.** What must be true — one testable sentence. (Serves Tier-1: which beat.)
-->

## 3. Build order (risk-first; binding, not suggestions)
1. <first slice> — gate **M0**
