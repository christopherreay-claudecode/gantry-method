# #0027 — mmt store is input-addressed: blobs are raw documents (hash = the document), linkification happens at view time from a per-tree symbols.js emitted by Python; viewer.js applies the table and owns no grammar
type: feature        status: open
refs: [10] [11]   opened: seed   closed-by: <sha>

**Constraints this issue makes true (Tier 2 — each traces to a plan constraint in `refs:`):**
<what must be true when this closes — one testable sentence per constraint; each traces to a plan constraint above>

**Depends on:**
<the issues this needs first — mirror them on the deps: line as blocks/awaits-stamp/defers-to/informs>

**Approaches (the technical ways to satisfy the constraints; the one taken, and why):**
<the technical ways considered; the one chosen and why; what a seam would let you swap later>

**Exit (what latches this):**
<the test / observation that latches this>
