# #0007 — M5: docs — SPEC §8 lineage & bands, docs/00-llm-playbook.md, READMEs, scripts/README
type: milestone        status: open
refs: [1] [4] [7] [m3]   opened: M0   closed-by: <sha>

**Component constraint (Tier 2):** an LLM that opens this repo finds, in the README's first
screen, the pointer to `docs/00-llm-playbook.md` — "your situation → the exact command" — and
SPEC §8 states the lineage law (levels are thousands, streams sub-band, bands enforced,
lineage visible) with the same precision the grammar sections have.
**Traces to (Tier 1):** "without knowing anything the commands did not tell it".
