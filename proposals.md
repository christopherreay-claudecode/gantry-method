# gantry — proposals (on the table, not in the graph)

## issues
- #0008 — retire fork-app.sh into gantry fork + a per-base manifest
  type: workorder        status: open
  refs: [1] [S1]   opened: seed
- #0009 — restore metalKnee-era rigor: state.schema.json + validate_deps.py
  type: workorder        status: open
  refs: [8]   opened: seed
- #0010 — REV includes generated_from (commit, extractor version): every code commit moves the fingerprint even when structure is identical — decide whether the REV should hash entities only
  type: ambiguity        status: open
  refs: [7]   opened: seed

## routes
- route A (finish the lineage story first): #0007 docs → #0008 → #0009; leave #0010 as a hold until a client feels the pain
