# gantry — proposals (on the table, not in the graph)

## issues
- #0012 — retire fork-app.sh into gantry fork + a per-base manifest
  type: workorder        status: open
  refs: [1] [S1]   opened: seed
- #0013 — restore metalKnee-era rigor: state.schema.json + validate_deps.py
  type: workorder        status: open
  refs: [8]   opened: seed
- #0014 — REV includes generated_from (commit, extractor version): every code commit moves the fingerprint even when structure is identical — decide whether the REV should hash entities only
  type: ambiguity        status: open
  refs: [7]   opened: seed

## routes
- route A (finish the lineage story first): #0012 → #0013; leave #0014 as a hold until a client feels the pain
