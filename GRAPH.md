# GRAPH — gantry @ a603373+dirty · REV 01fcb550b0da
# generated by gantry extract — do NOT hand-edit; to change it, change the
# issues and re-run extract. Detail lives in the issue files; this is the map.

lineage: level 0 · root · issues #0001–#0999

gates latched:  · open: m0 m1 m2 m3 m4 m5 · phases open: 
seams provisional: S1

open items (kind · refs → upstream anchors):
  #0001 workorder m0-bootstrap → m0,10
  #0002 workorder m1-entry-point → 1,2,3,m0,m1
  #0003 workorder m2-extractor-lineage → 6,7,8,9,m1,m2
  #0004 workorder m3-fork-stream → 4,5,6,7,m2,m3
  #0005 workorder m4-tests → 11,m3,m4
  #0006 hold      stream-band-size ⛭human → 5
  #0007 workorder m5-docs → 1,4,7,m3,m5

dependency edges (blocks = must resolve first):
  #0002 —blocks→ #0004 [parsed]
  #0002 —informs→ #0003 [parsed]
  #0003 —blocks→ #0004 [parsed]
  #0004 —defers-to→ #0006 [parsed]
  #0004 —informs→ #0007 [parsed]
  #0006 —informs→ #0004 [parsed]

closed/resolved: none

sources — where the full content lives:
  plan.md                                   → constraints 1–11
  seams.md                                  → seams S1
  proposals.md                              → proposed content (on the table)
  issues/0001-m0-bootstrap.md               → gate m0
  issues/0002-m1-entry-point.md             → gate m1
  issues/0003-m2-extractor-lineage.md       → gate m2
  issues/0004-m3-fork-stream.md             → gate m3
  issues/0005-m4-tests.md                   → gate m4
  issues/0006-ambiguity-stream-band-size.md → hold Q1 (stream-band-size)
  issues/0007-m5-docs.md                    → gate m5
## proposed — tentative, on the table, NOT part of the graph
# When accepted: copy into plan.md / seams.md / issues/ and re-run extract.

proposed issues (not real tracker items until filed):
  - #0008 retire fork-app.sh into gantry fork + a per-base manifest (workorder · refs [1] [S1])
  - #0009 restore metalKnee-era rigor: state.schema.json + validate_deps.py (workorder · refs [8])
  - #0010 REV includes generated_from (commit, extractor version): every code commit moves the fingerprint even when structure is identical — decide whether the REV should hash entities only ⛭human (ambiguity · refs [7])

proposed routes (prose combinations over the matrix — the LLM proposes, a human weighs):
  - route A (finish the lineage story first): #0007 docs → #0008 → #0009; leave #0010 as a hold until a client feels the pain

