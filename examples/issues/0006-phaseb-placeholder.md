# #0006 — Phase B (placeholder)
type: milestone        status: open
refs: [PhaseB] [S2]   opened: seed   closed-by: <all Phase-B gates latched>

Placeholder per the seeding rule: exactly one issue for a far phase, expanded into real milestone
issues only on phase entry (no speculative decomposition of unbuilt phases).

**Scope:** the real rigid-body press driver behind seam **S2** (`AnalyticPress` → `RigidBodyPress`),
which stays provisional until this lands. Named with a phase slug (`phaseb-…`), so extract yields
the open **phase `b`**; refs `[S2]` so it is anchored (not floating) even though `[PhaseB]`
resolves to itself.
