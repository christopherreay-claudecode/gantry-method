# GRAPH — widget-press (fullMethodology example) @ c0b19d1+dirty · REV 5d926ddbe8ee
# generated by gantry extract — do NOT hand-edit; to change it, change the
# issues and re-run extract. Detail lives in the issue files; this is the map.

gates latched: m0 · open: m1 · phases open: b
seams provisional: S2
seams frozen: S1

open items (kind · refs → upstream anchors):
  #0002 workorder m1-press-cycle → 3,S1,m0,m1
  #0004 hold      torque-law ⛭human → 4
  #0006 workorder phaseb-placeholder → b,S2

dependency edges (blocks = must resolve first):
  #0002 —defers-to→ #0004 [parsed]
  #0002 —informs→ #0003 [parsed]
  #0004 —defers-to→ #0002 [proposed]
  #0004 —informs→ #0002 [parsed]

closed/resolved: #0001 #0003 #0005
## proposed — tentative, on the table, NOT part of the graph
# When accepted: copy into plan.md / seams.md / issues/ and re-run extract.

decision matrix (promote exactly one alternative per decision):
  presets:
    - per-material: The press remembers the last good pressure curve per material.
    - per-operator: The press remembers the last good pressure curve per operator.
  auth:
    - passwordless-default: A new session starts passwordless when the machine allows it.
    - password-required: Every session requires the operator password.

proposed seams:
  - S3 telemetry-writer: local log → supabase telemetry; freeze at M2

proposed issues (not real tracker items until filed):
  - #1007 hold: material presets UX ⛭human (ambiguity · refs [3])

proposed routes (prose combinations over the matrix — the LLM proposes, a human weighs):
  - route A (defaults): presets[per-material] × auth[passwordless-default] × telemetry[local]
  - route B (conservative): presets[per-operator] × auth[password-required] × telemetry[supabase]

